<!--<p>*mettre du code ici pour mettre en forme une 'card'*</p>

<ul>
  <% @items.each do |item|  %>    
    <li>
        <p>Item.title = <%= item.title  %><br/>
        Item.image_url = <%= item.image_url  %><br/>
        Item.description = <%= item.description  %><br/>
        Item.price = <%= item.price  %><br/>     
        </p>   
    </li>
  <% end %>
</ul> -->

<div class="row mx-auto mt-5">
  <% @items.each do |item|  %>
    <div class="col-sm-4 w-25 mx-auto">
      <div class="card border-dark text-center">
    <img class="card-img-top" src="<%=item.image_url%>" alt="..." />
        <div class="card-body">
          <h5 class="card-title"><%=item.title%></h5>
          <p class="card-text"><%=item.price%>0€</p>
          <%= link_to "Voir", item_path(item), class:"btn btn-dark" %>
        </div>
      </div>
      <br/>
    </div>
  <% end %>
</div>         