  <% @items.each do |item|  %>
    *mettre du code ici pour mettre en forme une 'card'*
    <%= item.title  %>
    <%= item.image_url  %>
    <%= item.description  %>
    <%= item.price  %> 
      <% end %>